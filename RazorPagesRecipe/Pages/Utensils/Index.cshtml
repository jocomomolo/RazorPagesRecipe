@page
@model RazorPagesRecipe.Pages.Utensils.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Utensil[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Utensil) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.UtensilID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.UtensilID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.UtensilID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
